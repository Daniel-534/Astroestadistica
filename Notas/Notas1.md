# Duda (No del curso)
* Hacer la demostración del valor esperado de el estimador $\hat{\theta} = max(X_1,X_2,...,X_n)$ que nos dice que es $n/(n+1)\theta$ y hacer una figura para ver por qué razón este no es un estimador insesgado y el otro sí.

Hacer una tabla comparativa entre probabilidad y likelihood muy completa.

Se halló la estimación de un parámetro (p) de una fmp de bernoulli
mediante el método de maximum likelihood. El caso estudiado fue el ejemplo 1 del devore en la sección de estimacion por maxima verosimilitud. (Nota: Escribir aquí ese ejemplo pero suponiendo un caso de astronomía [Cometas])

Se obtuvo el estimador de los parámetros $\mu$ y $\sigma^2$ de una fdp normal
mediante el método de maximum likelihood de manera general (sin reemplazar números) para ver cómo funciona este método cuando tenemos más de 1 parámetro, en este caso son 2 parámetros.
