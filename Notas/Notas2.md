# Temas

* Estimación puntual.
* Método de estimación por momentos
* Momentos centrados E[(x-\mu)^k] con k=2
y_i conjunto de datos medidos como función de x_i
Se tiene un modelo M(x_i; theta) con \{\theta\} un vector de parámetros.
\mathbb{S} = \sum[y_i - M(x_i;\theta)]^2 -> minimizar \mathbb{S}
